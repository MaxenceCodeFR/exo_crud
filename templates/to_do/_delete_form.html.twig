<form method="post" action="{{ path('app_to_d_o_delete', {'id': to_d_o.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ to_d_o.id) }}">
    <button class="btn">Delete</button>
</form>
